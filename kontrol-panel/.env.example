# Panel'in çalışacağı port
PANEL_PORT=4500

# JWT şifreleme anahtarı - UZUN VE RASTGELE BİR STRİNG YAZ
# Örnek üretmek için: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=lutfen_bunu_uzun_rastgele_bir_stringle_degistir_super_gizli_olsun
